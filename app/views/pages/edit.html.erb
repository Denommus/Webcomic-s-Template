<h1>Editing page</h1>

<%= form_for @page, :url => @page, :html => { :multipart => true } do |f| %>
  <%= render 'form', :f => f %>
<% end %>

<%= link_to 'Show', @page %> |
<%= link_to 'Back', pages_path %>
